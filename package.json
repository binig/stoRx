{
  "name": "Maginot",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "npm run build:commonjs",
    "build:commonjs": "cross-env BABEL_ENV=commonjs babel src --out-dir lib",
    "clean": "rimraf lib coverage esdoc",
    "doc": "mkdirp esdoc && esdoc -c esdoc.json",
    "lint": "eslint src test examples",
    "report": "npm run test && nyc report --reporter=lcov && codecov",
    "test:unit": "tsc --outDir dist/test && ava",
    "test:e2e": "node test/e2e/runner.js",
    "test": "npm run lint && npm run test:unit &&  npm run test:e2e",
    "prepublish": "npm run clean && npm run report && npm run build && npm run doc"
  },
  "devDependencies": {
    "@types/node": "^8.0.14",
    "ava": "^0.21.0",
    "chromedriver": "^2.30.1",
    "ts-helper": "^0.0.1",
    "ts-loader": "^2.3.0",
    "ts-node": "^3.2.1",
    "typescript": "^2.4.1",
    "webpack": "^3.3.0"
  },
  "dependencies": {
    "rxjs": "^5.4.2"
  },
  "ava": {
    "files": [
      "dist/test/test/unit/**/*.spec.js"
    ],
    "source": [
      "dist/test/src/**/*.js"
    ],
    "concurrency": 5,
    "failFast": true,
    "tap": false,
    "powerAssert": false,
    "babel": "inherit",
    "require": [
      "babel-register"
    ]
  }
}
